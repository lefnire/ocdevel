{
  "name": "ocdevel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "AWS_PROFILE=admin AWS_REGION=us-east-1 sst dev",
    "build": "AWS_PROFILE=admin AWS_REGION=us-east-1 sst build",
    "deploy": "AWS_PROFILE=admin AWS_REGION=us-east-1 sst deploy --stage=prod",
    "remove": "AWS_PROFILE=admin AWS_REGION=us-east-1 sst remove",
    "remove:prod": "AWS_PROFILE=admin AWS_REGION=us-east-1 sst remove --stage=prod",
    "console": "AWS_PROFILE=admin AWS_REGION=us-east-1 sst console",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "sst": "2.40.3",
    "aws-cdk-lib": "2.124.0",
    "constructs": "10.3.0",
    "typescript": "^5.3.3",
    "@tsconfig/node18": "^18.2.2"
  },
  "workspaces": [
    "packages/*"
  ]
}